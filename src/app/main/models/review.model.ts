import { SafeResourceUrl, SafeHtml } from '@angular/platform-browser';

export class Review {
    public image: string;
    public name: string;
    public text: string;
    public videoLink: SafeHtml;
}